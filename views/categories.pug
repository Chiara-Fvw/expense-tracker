extends layout

block main 
  section#categories
  header 
    h1 Categories 
    ul 
      each category in categories
        li
          a(href=`/expenses/${category.id}`)
            h2= category.title